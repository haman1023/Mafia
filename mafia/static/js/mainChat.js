// function mainChatSend(event) {
	
// }
// mainChatInput.addEventListener('keydown', mainChatSend(event));

// 필요 변수 가져오기
const mainChatInput = document.querySelector('main_chat_text');
const btnMainChat = document.querySelector('main_chat_send');
// 채팅창에 포커스를 주기 
mainChatInput.focus();

function init() {
	
	// mainChatInput.onkeydown = 
	// 메인채팅창 메시지 보내기
}
init();